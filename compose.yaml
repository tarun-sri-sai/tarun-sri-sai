services:
  devenv:
    command: bash
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    networks:
      - redis_net
    volumes:
      - ${PWD}:/app
    working_dir: /app
networks:
  redis_net:
    external: true
